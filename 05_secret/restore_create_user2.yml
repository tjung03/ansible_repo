---
- name: Restore create_user{,2}.yml
  hosts: all
  become: true
  become_user: root
  vars_files:
    - secret2.yml
  tasks:
    - name: 1) 사용자 추가
      ansible.builtin.user:
        name: "{{ username }}"
        state: absent
        remove: true
