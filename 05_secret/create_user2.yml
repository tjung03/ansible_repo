---
- name: 0) 사용자 추가
  hosts: web2.example.com
  become: true
  become_user: root
  vars_files:
    - secret2.yml
  tasks:
    - name: 1) 사용자 추가
      ansible.builtin.user:
        name: "{{ username }}"
        password: "{{ pwhash | password_hash('sha512') }}"
