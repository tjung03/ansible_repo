---
- name: Virtual Hosting
  hosts: localhost
  gather_facts: true
  tasks:
    - name: Virtual Hosting - /var/www/vhosts/{{ inventory_hostname }}/
      ansible.builtin.include_role:
        name: myvhost
